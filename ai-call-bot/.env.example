# AI Call Bot Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# TWILIO CONFIGURATION (Required)
# ===========================================
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890

# ===========================================
# ELEVENLABS CONVERSATIONAL AI (Required for Agent Mode)
# ===========================================
# API key for ElevenLabs - used for Conversational AI agents
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Default ElevenLabs Conversational AI Agent ID
# Create an agent at: https://elevenlabs.io/conversational-ai
# When set, calls will use agent-based mode by default
ELEVENLABS_AGENT_ID=your_default_agent_id

# ElevenLabs Twilio Phone Number ID (Required for outbound calls)
# Get from ElevenLabs dashboard after importing your Twilio phone number:
# 1. Go to https://elevenlabs.io/app/conversational-ai
# 2. Navigate to "Phone Numbers"
# 3. Click "Import Twilio Number"
# 4. Enter Twilio Account SID, Auth Token, and phone number
# 5. Copy the generated Phone Number ID (format: pn_xxxxxxxxx)
ELEVENLABS_PHONE_NUMBER_ID=your_phone_number_id

# ===========================================
# OPENAI CONFIGURATION (Optional - Legacy Mode Only)
# ===========================================
# Only needed if NOT using ElevenLabs Conversational AI agents
# Used for conversation AI in legacy voice-based mode
OPENAI_API_KEY=your_openai_api_key_here

# ===========================================
# LEGACY TTS PROVIDER SELECTION (Optional)
# ===========================================
# Only applies when NOT using agent mode (no agentId provided)
# Options: 'elevenlabs' (premium, $0.20/min) or 'openai' (cost-effective, $0.015/min)
# Default: elevenlabs
DEFAULT_TTS_PROVIDER=elevenlabs

# Default ElevenLabs voice ID for legacy mode (optional)
# Default: 4tRn1lSkEn13EVTuqb0g (Serafina)
DEFAULT_VOICE_ID=4tRn1lSkEn13EVTuqb0g

# ===========================================
# SERVER CONFIGURATION (Optional)
# ===========================================
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Logging level: error, warn, info, verbose, debug, silly
LOG_LEVEL=info

# ===========================================
# RAILWAY DEPLOYMENT (Optional)
# ===========================================
# These are automatically set by Railway
# RAILWAY_PUBLIC_DOMAIN=your-app.railway.app
# RAILWAY_STATIC_URL=https://your-app.railway.app

# ===========================================
# CALL TRACKING (Optional)
# ===========================================
# Maximum age of calls to keep in tracker (in milliseconds)
# Default: 1800000 (30 minutes)
CALL_TRACKER_MAX_AGE_MS=1800000

# ===========================================
# ARCHITECTURE COMPARISON
# ===========================================
# 
# RECOMMENDED: ElevenLabs Conversational AI (Agent Mode)
# =====================================================
# Flow: Twilio Call → ElevenLabs Conversational AI Agent → Twilio
# Latency: <100ms
# Cost: ~$0.10/min
# Setup: Create agent at elevenlabs.io, set ELEVENLABS_AGENT_ID
# 
# LEGACY: Voice-based Mode (OpenAI + TTS)
# =======================================
# Flow: Twilio Call → STT → OpenAI GPT → ElevenLabs TTS → Twilio
# Latency: ~2.5s
# Cost: ~$0.22/min
# Setup: Set OPENAI_API_KEY (no ELEVENLABS_AGENT_ID)
#
# Cost Example (1,000 calls × 5 minutes each):
#   - Agent Mode: $500/month
#   - Legacy Mode: $1,100/month
#   - Savings: $600/month (55%)

